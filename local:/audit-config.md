## /Users/tommasoprinetti/Documents/spinosa-main/packages/tui/src/theme/index.ts
theme/index.ts:36-93:23 of 55 Theme type fields (42%) are never used in getSyntaxRules | impact:medium | category:dead
theme/index.ts:601-1104:getSyntaxRules is a 505-line hardcoded array (46% of file) of repetitive scope→style mappings — could be JSON or generated from a compact DSL | impact:medium | category:over
theme/index.ts:36-93:Unused Theme fields: primary, selectedListItemText, backgroundPanel, backgroundElement, backgroundMenu, border, borderActive, borderSubtle, diffHunkHeader, diffHighlightAdded, diffHighlightRemoved, diffLineNumber, diffAddedLineNumberBg, diffRemovedLineNumberBg, markdownText, markdownHorizontalRule, markdownListEnumeration, markdownImage, markdownImageText, markdownCodeBlock, spinnerColor, inactiveFactor, thinkingOpacity | impact:medium | category:dead
theme/index.ts:316-359:ansiToRgba (44 lines) reimplements terminal ANSI→RGBA mapping — ntc or oclib would be lighter | impact:low | category:over
theme/index.ts:134-168:DEFAULT_THEMES inline JSON in JS source instead of separate .json imports (except zenburn on line 34 which IS a separate import) | impact:low | category:over
theme/index.ts:170-192:pluginThemes/customThemes/systemTheme/listeners + listThemes/syncThemes — mutable global state with listeners for theme changes, but only 3 exported mutation functions (setCustomThemes, setSystemTheme) ever call syncThemes | impact:low | category:over
theme/index.ts:486-569:generateGrayScale + generateMutedTextColor (83 lines combined) only called once from generateSystem (110 lines) which itself is only called once | impact:low | category:over

## /Users/tommasoprinetti/Documents/spinosa-main/packages/tui/src/config/keybind.ts
keybind.ts:45-239:Definitions declares 182 keybind names, 42 bound to "none" (inaccessible without user override) | impact:medium | category:dead
keybind.ts:45-239:Definitions entries with no corresponding CommandMap entry (20 orphans): dialog.select.prev, dialog.select.next, dialog.select.page_up, dialog.select.page_down, dialog.select.home, dialog.select.end, dialog.select.submit, dialog.prompt.submit, dialog.mcp.toggle, dialog.move_session.new, dialog.move_session.delete, dialog.move_session.refresh, prompt.autocomplete.prev, prompt.autocomplete.next, prompt.autocomplete.hide, prompt.autocomplete.select, prompt.autocomplete.complete, permission.prompt.fullscreen, plugins.toggle, dialog.plugins.install — these keybinds can never trigger a command | impact:high | category:break
keybind.ts:244-254:KeybindOverrides Schema dynamically generated by iterating Definitions, then Descriptions and CommandDescriptions re-iterate Definitions — 3 iterations of the same 182-entry object for 3 different derived forms | impact:medium | category:over
keybind.ts:447-456:parse() runs unknownKeys validation + Object.fromEntries map over 182 entries — Schema validation on top of the dynamic Schema.Struct generated on line 244 | impact:low | category:over
keybind.ts:6:Uses effect/Schema for what is essentially a string→(string|object) binding definition — adds heavy dependency for trivial parsing | impact:medium | category:over
keybind.ts:470:470-line file where >50% is the Definitions array (195 lines) and CommandMap (163 lines) — pure data masquerading as code | impact:low | category:over
keybind.ts:460-462:unknownKeys() validates input against KeybindNames Set, but parse() already does Schema validation via KeybindOverrides which also rejects unknown keys — double validation | impact:low | category:over

## /Users/tommasoprinetti/Documents/spinosa-main/packages/tui/src/keymap.tsx
keymap.tsx:136-173:inputCommands array (37 entries) duplicates command strings already present in config/keybind.ts CommandMap — if a new input command is added in one place but not the other, it silently breaks | impact:medium | category:dup
keymap.tsx:20-22:LEADER_TOKEN, OPENCODE_BASE_MODE, COMMAND_PALETTE_COMMAND exported constants but COMMAND_PALETTE_COMMAND is also a value in CommandMap ("command.palette.show" on keybind.ts:265) — duplicate string | impact:low | category:dup
keymap.tsx:1-18:Imports both from @opentui/keymap/* and local ./config/keybind — two layers of keybinding abstraction (OpenTUI engine + local config schema) creating two things to keep in sync | impact:medium | category:dup
keymap.tsx:206-212:formatKeySequence/formatKeyBindings are one-liner wrappers delegating to @opentui extras — add nothing beyond defaulting formatOptions | impact:low | category:over
keymap.tsx:214-244:registerOpencodeKeymap calls 8 different registration functions and returns a cleanup closure — high coupling to OpenTUI internal addons API | impact:low | category:over

## /Users/tommasoprinetti/Documents/spinosa-main/packages/tui/src/config/index.tsx
config/index.tsx:4:Uses effect/Schema for a config struct with 13 optional fields — Schema.Struct + Schema.Literals + Schema.Record for what JSON.parse + a 30-line type guard could handle | impact:medium | category:over
config/index.tsx:88-117:resolve() merges defaults and runs createBindingLookup + TuiKeybind.parse + TuiKeybind.toBindingConfig — layers of parsing/wrapping for a one-level config override | impact:medium | category:over
config/index.tsx:8-15:AttentionSoundName Schema.Literals — 5 string literals as a Schema when a union type would suffice | impact:low | category:over

## /Users/tommasoprinetti/Documents/spinosa-main/packages/tui/src/clipboard.ts
clipboard.ts:9-21:command() wrapper reimplements execFile via spawn manually (stdio piping, buffer accumulation, error handling) while also importing promisify(execFile) on line 7 — two parallel subprocess patterns in the same file | impact:medium | category:over
clipboard.ts:7:promisify(execFile) imported and used as `exec()` but the command() function on line 9 duplicates its functionality with spawn — inconsistent | impact:low | category:over
clipboard.ts:96-118:getCopyMethod() lazy-loads @opencode-ai/core/util/which at runtime (dynamic import) for platform detection already done in copyCommand() — two platform detection paths | impact:low | category:over
clipboard.ts:120-124:write() calls writeOsc52 + getCopyMethod() — always writes via OSC52 AND a native command AND may fall through to clipboardy (dynamic import), meaning 2-3 clipboard writes per invocation | impact:medium | category:over
clipboard.ts:71:clipboardy npm package used as fallback — if clipboardy can do all the work, the entire platform branch logic (lines 30-69, 76-94, 98-118) is redundant | impact:medium | category:over

## /Users/tommasoprinetti/Documents/spinosa-main/packages/tui/src/audio.ts
audio.ts:7-21:getAudio() creates Audio.create({ autoStart: false }) from @opentui/core — adds TUI audio abstraction layer for what browser/TUI contexts handle natively | impact:low | category:over
audio.ts:23-36:loadSoundFile() caches promises in a Map, reads file with fs, then calls current.loadSound(bytes) — sound file read through Node fs when TUI Audio API may have its own loading | impact:low | category:over

## /Users/tommasoprinetti/Documents/spinosa-main/packages/tui/src/config/ vs /packages/tui/src/keymap.tsx
config/keybind.ts + config/index.tsx + keymap.tsx:Three files sharing responsibility for keybinding definitions, command mapping, and keymap registration — Definitions in keybind.ts, CommandMap in keybind.ts, inputCommands duplicated in keymap.tsx, config resolution in index.tsx, runtime registration in keymap.tsx | impact:high | category:dup
config/keybind.ts:255-418 vs keymap.tsx:136-173:CommandMap in keybind.ts maps 162 definition names to dotted command strings; keymap.tsx hardcodes 37 of those same command strings as inputCommands — any mismatch silently disables the binding | impact:high | category:break
