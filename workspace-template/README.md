# Spinosa workspace

Local-first research workspace generated by Spinosa. Start with `startup-prompt.md`; keep source material in `raw/`, navigation in `maps/`, and evidence-backed reports in `agent_reports/`.

Framework-managed files update with `spinosa update`. User state under `raw/`, `system/`, and `.spinosa/memory/` is preserved.
